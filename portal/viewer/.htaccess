AuthType Basic
AuthName "Restricted Area"
AuthUserFile /home/teezecrost/.htpasswd

# Require BOTH valid login AND matching IP (belt + suspenders)
<RequireAll>
    Require valid-user
    Require ip 24.78.157.169
</RequireAll>
